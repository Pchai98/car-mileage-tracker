# Car Mileage Tracker

## Overview
This project tracks car mileage for various purposes.

## Installation
To install the necessary dependencies, use:

```bash
pip install -r requirements.txt
```

## Usage
Run the application with:

```bash
python app.py
```